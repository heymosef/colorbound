# Colorbound

Colorbound is a React and Vite app for building, previewing, exporting, and sharing color token palettes.

## Running the app

Run `npm install` to install dependencies.

Run `npm run dev` to start the development server.

Run `npm test` to execute the Vitest suite.

Run `npm run build` to create a production bundle.
